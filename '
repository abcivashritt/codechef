#include <iostream>
#include <vector>
using namespace std;

int main() {
	vector < int > primes;
	vector < bool > is_prime(100001, true);
	for(int i=2;i<100001;i++) {
		if(is_prime[i]) {
			primes.push_back(i);
			for(int j=i*i;j<100001;j+=2)
				is_prime[j] = false;
		}
	}
	int t;
	cin>>t;
	while(t--) {
		int m,n;
		cin>>m>>n;
		vector < int > big_primes;
		is_prime.resize(n-m+2, true);
		for(int i=0;i<primes.size();i++) {
			for(int j=(((m+primes[i]-1)/primes[i])*primes[i]);j<=n;j+=primes[i]) {
				is_prime[j-m] = false;
			}
		}
		for(int i=m;i<=n;i++) {
			if(is_prime[i-m])
				cout<<i<<"\n";
		}
		cout<<"\n";
	}
	return 0;
}
